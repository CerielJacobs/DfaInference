ARGS=
# ARGS="$ARGS -DIncrementalCounts"
# ARGS="$ARGS -DCompensateRedundancy"
# ARGS="$ARGS -DMissingEdges"
# ARGS="$ARGS -DUseProductive"

prun -t 0:10:0 -4 CLASSPATH=../../DfaInference.jar:. -v -o learn-7.out \
    -rsh ssh \
    $IBIS_HOME/bin/ibis-prun $1 \
    -server -Xms512M -Xmx512M \
    -Dsatin.closed \
    -Dibis.name=tcp \
    -Dsatin.alg=MW \
    $ARGS \
    DfaInference.SatinFolder \
    -input ../train.7 -depth 5 -mw -window 192 -dump DfaDump7
