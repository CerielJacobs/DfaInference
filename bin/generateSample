#! /bin/sh

# generateSample <DFA> <count> <maxlength>
# Generates a sample from <DFA>, with <count> sentences, of maximum length
# <maxlength>.

$SATIN_HOME/bin/ibis-satin-run -Xmx256M abbadingo.GenerateSampleAndTest -m $1 -count $2 -maxl $3 -prefix $1
