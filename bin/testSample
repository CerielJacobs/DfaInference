#! /bin/sh
 
# testSample <LearnedDFA> <sample> <RefDFA>
# Runs the sample through the LearnedDFA and the GeneratingDFA, and prints
# the correctness ratio.

$SATIN_HOME/bin/ibis-satin-run -Xmx256M test.TestSample -m $1 -s $2 -base $3
