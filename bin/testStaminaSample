#! /bin/sh
 
# testStaminaSample <LearnedDFA> <sample>
# Runs the sample through the LearnedDFA and prints a 1 or 0
# for each sentence.

bin/satin-run -Xmx256M test.TestSample -m $1 -s $2 -reader stamina.StaminaReader
